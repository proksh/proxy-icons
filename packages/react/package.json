{
  "name": "@proxy-icons/react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate-src": "npm explore @proxy-icons/generate-icons -- npm run generate-icons -- --cwd=$(pwd)",
    "build": "rimraf dist && tsdx build --format es,cjs --entry src/index.tsx",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@proxy-icons/generate-icons": "file:../generate-icons"
  }
}
