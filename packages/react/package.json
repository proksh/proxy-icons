{
  "name": "@proxy-icons/react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate-src": "generate-icons",
    "build": "rimraf dist && tsdx build --format es,cjs --entry src/index.tsx",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "generate-icons": "*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.7",
    "tsdx": "^0.14.1"
  }
}
